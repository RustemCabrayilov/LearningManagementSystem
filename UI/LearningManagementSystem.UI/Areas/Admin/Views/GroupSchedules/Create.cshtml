@model LearningManagementSystem.Application.Abstractions.Services.GroupSchedule.GroupScheduleRequest


<div class="col-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <form class="forms-sample" asp-action="Create">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="DayOfWeek" class="control-label">
                        Day
                    </label>
                    <select asp-for="DayOfWeek" class="form-control" asp-items="@(new SelectList(ViewBag.DayList))">
                        <option>Please select one</option>
                    </select>
                    <span asp-validation-for="DayOfWeek" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="ClassTime" class="control-label">End Date</label>
                    <select asp-for="ClassTime"
                            class="form-control"
                            asp-items="@(new SelectList(ViewBag.TimeList))">
                        <option>Please select one</option>
                    </select>
                    <span asp-validation-for="ClassTime" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="GroupId" class="control-label">Group</label>
                    <select asp-for="GroupId"
                            class="form-control"
                            asp-items="@(new SelectList(ViewBag.Groups,"Id","Name"))">
                        <option>Please select one</option>
                    </select>
                    <span asp-validation-for="GroupId" class="text-danger"></span>
                </div>
                <button type="submit" class="btn btn-gradient-primary me-2">Submit</button>
                <button class="btn btn-light" asp-action="Index">Cancel</button>
            </form>
        </div>
    </div>
</div>